import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  label: {
    textAlign: 'center',
    marginVertical: 10
  },
  input: {
    minWidth: 150,
    height: 40,
    borderRadius: 5,
    backgroundColor: 'lightgray',
    paddingVertical: 0,
    paddingHorizontal: 8
  }
})
